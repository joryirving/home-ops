---
# yaml-language-server: $schema=https://raw.githubusercontent.com/bjw-s-labs/helm-charts/main/charts/other/app-template/schemas/helmrelease-helm-v2.schema.json
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: movie-roulette
spec:
  chartRef:
    kind: OCIRepository
    name: app-template
  dependsOn: []
  interval: 15m
  values:
    controllers:
      movie-roulette:
        annotations:
          reloader.stakater.com/auto: "true"
        containers:
          app:
            image:
              repository: ghcr.io/sahara101/movie-roulette
              tag: v5.1.2@sha256:acf88aed7a2e7808077f9a4992ad32d4dfb59c1c095059b1280dba50a4287b89
            env:
              #Homepage ENV
              HOMEPAGE_MODE: "FALSE"
              #Plex ENV
              PLEX_URL: https://plex.jory.dev
              PLEX_MOVIE_LIBRARIES: "Movies" #Default movies, add more with comma delimiter A,B,C
              #Poster ENV
              TZ: America/Edmonton
              DEFAULT_POSTER_TEXT: "Smurf Cinema"
              #Client ENV
              # APPLE_TV_ID: "" #https://github.com/sahara101/Movie-Roulette#apple-tv
              #Miscellaneous
              USE_LINKS: TRUE
              USE_FILTER: TRUE
              USE_WATCH_BUTTON: TRUE
              USE_NEXT_BUTTON: TRUE
            envFrom:
              - secretRef:
                  name: movie-roulette-secret
            resources:
              requests:
                cpu: 15m
                memory: 180M
              limits:
                memory: 300M
    service:
      app:
        ports:
          http:
            port: 4000
    route:
      app:
        hostnames: ["{{ .Release.Name }}.jory.dev"]
        parentRefs:
          - name: envoy-external
            namespace: network
    persistence:
      config:
        existingClaim: "{{ .Release.Name }}"
        globalMounts:
          - path: /app/data
