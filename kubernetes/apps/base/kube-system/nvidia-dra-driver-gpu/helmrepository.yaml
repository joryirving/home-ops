---
# yaml-language-server: $schema=https://k8s-schemas.home-operations.com/source.toolkit.fluxcd.io/helmrepository_v1.json
apiVersion: source.toolkit.fluxcd.io/v1
kind: HelmRepository
metadata:
  name: nvidia-dra-driver-gpu
spec:
  interval: 1h
  # Trailing slash required so the index's relative chart URLs (charts/*.tgz)
  # resolve under /nvidia/ rather than the domain root.
  url: https://helm.ngc.nvidia.com/nvidia/
