---
# yaml-language-server: $schema=https://json.schemastore.org/kustomization
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ./helmrelease.yaml
  - ./ocirepository.yaml
  - ./pvc.yaml
configMapGenerator:
  - name: acars-config
    options:
      disableNameSuffixHash: true
    files:
      - config.yaml=resources/acars-processor.yaml
  - name: acars-adsb-shim
    options:
      disableNameSuffixHash: true
    files:
      - shim.py=resources/adsb-shim.py
