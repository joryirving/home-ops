apiVersion: gateway.networking.k8s.io/v1alpha2
kind: UDPRoute
metadata:
  name: minecraft-udp-route
  namespace: mc-router
spec:
  parentRefs:
    - name: external
      namespace: kube-system
  rules:
    - backendRefs:
        - name: mc-router
          port: 25565
          kind: Service
          namespace: games
