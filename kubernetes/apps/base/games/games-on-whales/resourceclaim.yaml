---
apiVersion: resource.k8s.io/v1
kind: ResourceClaimTemplate
metadata:
  name: gow-amd-gpu
spec:
  spec:
    devices:
      requests:
        - name: gpu
          exactly:
            adminAccess: true
            allocationMode: All
            deviceClassName: gpu.amd.com
