---
# yaml-language-server: $schema=https://raw.githubusercontent.com/bjw-s-labs/helm-charts/main/charts/other/app-template/schemas/helmrelease-helm-v2.schema.json
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: &app hoymiles-exporter
spec:
  chartRef:
    kind: OCIRepository
    name: app-template
  interval: 15m
  dependsOn: []
  values:
    controllers:
      hoymiles-exporter:
        annotations:
          reloader.stakater.com/auto: "true"
        containers:
          app:
            image:
              repository: ghcr.io/joryirving/hoymiles-exporter
              tag: 0.1.0@sha256:ea67010d4bed6a8b3b28c7710f7022e9568b057d42f2231e62c96944ea5c6824
            env:
              DTU_HOST: solar.internal
              TZ: America/Edmonton
            probes:
              liveness:
                enabled: true
              readiness:
                enabled: true
              startup:
                enabled: true
            resources:
              limits:
                cpu: 100m
                memory: 200Mi
    service:
      app:
        ports:
          metrics:
            enabled: true
            protocol: TCP
            port: 9099
        annotations:
          external-dns.alpha.kubernetes.io/hostname: exporters.jory.dev
