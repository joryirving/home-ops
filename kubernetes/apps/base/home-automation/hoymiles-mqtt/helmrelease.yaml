---
# yaml-language-server: $schema=https://raw.githubusercontent.com/bjw-s-labs/helm-charts/main/charts/other/app-template/schemas/helmrelease-helm-v2.schema.json
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: hoymiles-mqtt
spec:
  chartRef:
    kind: OCIRepository
    name: hoymiles-mqtt
  interval: 15m
  dependsOn:
    - name: mosquitto
  values:
    defaultPodOptions:
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
        runAsGroup: 100
        fsGroup: 100
        fsGroupChangePolicy: OnRootMismatch
    controllers:
      hoymiles-mqtt:
        containers:
          app:
            image:
              repository: ghcr.io/joryirving/hoymiles-mqtt
              tag: 0.11.0@sha256:a391615b36d6d156089f950ac40e31e26b9da37e04712bf44bd087060c1cfd2a
            env:
              TZ: America/Edmonton
              DTU_HOST: solar.internal
              LOG_TO_CONSOLE: true
              MQTT_BROKER: mosquitto
            resources:
              requests:
                cpu: 10m
              limits:
                memory: 128Mi
            securityContext:
              allowPrivilegeEscalation: false
              readOnlyRootFilesystem: true
              capabilities: { drop: ["ALL"] }
