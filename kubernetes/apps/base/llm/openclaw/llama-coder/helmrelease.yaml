---
# yaml-language-server: $schema=https://raw.githubusercontent.com/bjw-s-labs/helm-charts/main/charts/other/app-template/schemas/helmrelease-helm-v2.schema.json
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: llama-coder
spec:
  chartRef:
    kind: OCIRepository
    name: app-template
  dependsOn: []
  interval: 15m
  values:
    controllers:
      llama-coder:
        annotations:
          reloader.stakater.com/auto: "true"
        initContainers:
          model-download:
            image:
              repository: python
              tag: 3.14-slim
            command:
              - /bin/sh
              - -ec
              - |
                set -euo pipefail

                if [ ! -s /models/qwen3-coder-next/Qwen3-Coder-Next-UD-Q4_K_XL.gguf ]; then
                  pip install --no-cache-dir huggingface_hub
                  mkdir -p /models/qwen35b
                  rm -rf /models/qwen3-coder-next/.cache/huggingface/download/Qwen3-Coder-Next-UD-Q4_K_XL.gguf.lock
                  hf download \
                    unsloth/Qwen3-Coder-Next-GGUF \
                    Qwen3-Coder-Next-UD-Q4_K_XL.gguf \
                    --local-dir /models/qwen3-coder-next

                  echo "Qwen3-Coder-Next GGUF download complete"
                else
                  echo "Qwen3-Coder-Next GGUF already downloaded, skipping"
                fi
            env:
              HF_HUB_ENABLE_HF_TRANSFER: "1"
            envFrom:
              - secretRef:
                  name: huggingface
            resources:
              requests:
                cpu: 250m
                memory: 512Mi
              limits:
                memory: 4Gi
        containers:
          app:
            image:
              repository: docker.io/kyuz0/amd-strix-halo-toolboxes
              tag: vulkan-radv@sha256:1d0722d70a65e1916e73cb93267c021613257d149dcde3273d1eecf5e815f873
            securityContext:
              privileged: true
            command:
              - llama-server
            args:
              - --host
              - 0.0.0.0
              - --port
              - "8080"
              - --alias
              - qwen3-coder-next
              - --model
              - /models/qwen3-coder-next/Qwen3-Coder-Next-UD-Q4_K_XL.gguf
              - --ctx-size
              - "100000"
              - --n-gpu-layers
              - "99"
              - --flash-attn
              - "on"
              - --parallel
              - "1"
              - --cont-batching
              - -sps
              - "0.90"
              - --no-context-shift
              - --cache-prompt
              - --kv-unified
              - --temp
              - "0.2"
              - --top-p
              - "0.95"
              - --top-k
              - "20"
              - --min-p
              - "0"
              - --cache-type-k
              - q8_0
              - --cache-type-v
              - q8_0
              - --batch-size
              - "2048"
              - --ubatch-size
              - "2048"
              - --no-mmap
              - --jinja
            # env:
            #   HSA_OVERRIDE_GFX_VERSION: "11.5.1"
            #   HSA_ENABLE_SDMA: "0"
            #   ROCBLAS_USE_HIPBLASLT: "1"
            #   HIP_VISIBLE_DEVICES: "0"
            resources:
              requests:
                cpu: 1
                memory: 40Gi
              limits:
                memory: 80Gi
            probes:
              liveness:
                enabled: true
                custom: true
                spec:
                  httpGet:
                    path: /health
                    port: 8080
                  initialDelaySeconds: 30
                  periodSeconds: 30
                  failureThreshold: 6
              readiness:
                enabled: true
                custom: true
                spec:
                  httpGet:
                    path: /health
                    port: 8080
                  initialDelaySeconds: 20
                  periodSeconds: 10
                  failureThreshold: 6
              startup:
                enabled: true
                custom: true
                spec:
                  httpGet:
                    path: /health
                    port: 8080
                  failureThreshold: 360
                  periodSeconds: 10
    defaultPodOptions:
      hostIPC: true
      nodeSelector:
        topology.kubernetes.io/gpus: amd
      tolerations:
        - key: llm-workload
          operator: Exists
          effect: NoSchedule
    service:
      app:
        type: LoadBalancer
        annotations:
          lbipam.cilium.io/ips: 10.69.10.21
        externalTrafficPolicy: Cluster
        ports:
          http:
            port: 8080
    persistence:
      models:
        existingClaim: llm-models
        globalMounts:
          - path: /models
      dev-dri:
        type: hostPath
        hostPath: /dev/dri
        globalMounts:
          - path: /dev/dri
