---
# yaml-language-server: $schema=https://kube-schemas.pages.dev/monitoring.coreos.com/servicemonitor_v1.json
apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: llama-vision
spec:
  selector:
    matchLabels:
      app.kubernetes.io/name: llama-vision
      app.kubernetes.io/instance: llama-vision
  namespaceSelector:
    matchNames:
      - llm
  targetLabels: ["app.kubernetes.io/name", "app.kubernetes.io/instance"]
  endpoints:
    - port: http
      path: /metrics
  fallbackScrapeProtocol: PrometheusText0.0.4
