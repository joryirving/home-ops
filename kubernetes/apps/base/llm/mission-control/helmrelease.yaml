---
# yaml-language-server: $schema=https://raw.githubusercontent.com/bjw-s-labs/helm-charts/main/charts/other/app-template/schemas/helmrelease-helm-v2.schema.json
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: &app mission-control
spec:
  chartRef:
    kind: OCIRepository
    name: app-template
  dependsOn: []
  interval: 15m
  values:
    defaultPodOptions:
      securityContext:
        runAsUser: 1000
        runAsGroup: 100
        fsGroup: 100
        fsGroupChangePolicy: OnRootMismatch
    controllers:
      mission-control:
        annotations:
          reloader.stakater.com/auto: "true"
        containers:
          app:
            image:
              repository: ghcr.io/joryirving/mission-control
              tag: 1.3.0@sha256:44e4504163ea8243c6839741c3092445a74c3a7596a46953ad37d252c98929dc 
            env:
              PORT: "3000"
              OPENCLAW_GATEWAY_HOST: "openclaw.llm"
              OPENCLAW_GATEWAY_PORT: "18789"
              NEXT_PUBLIC_GATEWAY_HOST: "miso.jory.dev"
              NEXT_PUBLIC_GATEWAY_PORT: "443"
              NEXT_PUBLIC_GATEWAY_PROTOCOL: "wss"
              NEXT_PUBLIC_GATEWAY_URL: "wss://miso.jory.dev"
              MC_ALLOWED_HOSTS: "mission-control.jory.dev"
            envFrom:
              - secretRef:
                  name: *app
            resources:
              requests:
                cpu: 100m
                memory: 256Mi
              limits:
                memory: 1Gi
    service:
      app:
        ports:
          http:
            port: 3000
    route:
      app:
        hostnames: ["mission-control.jory.dev"]
        parentRefs:
          - name: envoy-internal
            namespace: network
    persistence:
      data:
        existingClaim: "{{ .Release.Name }}"
        globalMounts:
          - path: /app/.data
