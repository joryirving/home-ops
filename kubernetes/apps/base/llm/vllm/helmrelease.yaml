---
# yaml-language-server: $schema=https://raw.githubusercontent.com/bjw-s-labs/helm-charts/main/charts/other/app-template/schemas/helmrelease-helm-v2.schema.json
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: vllm
spec:
  chartRef:
    kind: OCIRepository
    name: app-template
  dependsOn: []
  interval: 15m
  values:
    controllers:
      vllm:
        annotations:
          reloader.stakater.com/auto: "true"
        containers:
          app:
            image:
              repository: docker.io/kyuz0/vllm-therock-gfx1151
              tag: 20260309-141005@sha256:6e2b341dbf904df6bc07b73f9d1ecd9d03bf98a06d49342709e7796918d70942
            securityContext:
              privileged: true
            command:
              - /bin/bash
              - -lc
            args:
              - >-
                python -m vllm.entrypoints.openai.api_server
                --host 0.0.0.0
                --port 8080
                --model unsloth/Qwen3.5-122B-A10B-UD-Q4_K_XL
                --served-model-name qwen122b
                --max-model-len 300000
                --max-num-seqs 3
                --enable-prefix-caching
                --gpu-memory-utilization 0.95
                --dtype auto
                --trust-remote-code
                --limit-mm-per-prompt image=4
                --chat-template-content-format openai
            env:
              HSA_OVERRIDE_GFX_VERSION: "11.5.1"
              HSA_ENABLE_SDMA: "0"
              HIP_VISIBLE_DEVICES: "0"
              ROCR_VISIBLE_DEVICES: "0"
              HF_HOME: /models/huggingface
              VLLM_CACHE_ROOT: /models/vllm
            resources:
              requests:
                cpu: 1
                memory: 32Gi
              limits:
                memory: 115Gi
            probes:
              liveness:
                enabled: true
                custom: true
                spec:
                  httpGet:
                    path: /health
                    port: 8080
                  initialDelaySeconds: 30
                  periodSeconds: 30
                  failureThreshold: 6
              readiness:
                enabled: true
                custom: true
                spec:
                  httpGet:
                    path: /health
                    port: 8080
                  initialDelaySeconds: 20
                  periodSeconds: 10
                  failureThreshold: 6
              startup:
                enabled: true
                custom: true
                spec:
                  httpGet:
                    path: /health
                    port: 8080
                  failureThreshold: 180
                  periodSeconds: 10
    defaultPodOptions:
      hostIPC: true
      nodeSelector:
        topology.kubernetes.io/gpus: amd
      tolerations:
        - key: llm-workload
          operator: Exists
          effect: NoSchedule
    service:
      app:
        type: LoadBalancer
        annotations:
          lbipam.cilium.io/ips: 10.69.10.20
        externalTrafficPolicy: Cluster
        ports:
          http:
            port: 8080
    persistence:
      models:
        existingClaim: vllm-model-cache
        globalMounts:
          - path: /models
      dev-dri:
        type: hostPath
        hostPath: /dev/dri
        globalMounts:
          - path: /dev/dri
      dev-kfd:
        type: hostPath
        hostPath: /dev/kfd
        globalMounts:
          - path: /dev/kfd
