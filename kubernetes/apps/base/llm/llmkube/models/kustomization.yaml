---
# yaml-language-server: $schema=https://json.schemastore.org/kustomization
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ./pvc.yaml
  - ./servicemonitor.yaml
  - ./qwen3.6-27b.yaml
  # 2× 35B-Q5 (#strix-2x35b): two llama-strix-a/b instances (a=3 slots, b=2) own the
  # Strix; litellm simple-shuffle spreads `self-hosted` across them. 35B-Q8 alt off.
  # - ./llama-strix.yaml
  - ./llama-strix-35b-q5.yaml
