---
# yaml-language-server: $schema=https://json.schemastore.org/kustomization
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ./servicemonitor.yaml
  # nvidia slot (egpu / RTX 3090) — exactly one active:
  - ./qwen3.6-27b.yaml
  # - ./gemma-4-26b-a4b.yaml
