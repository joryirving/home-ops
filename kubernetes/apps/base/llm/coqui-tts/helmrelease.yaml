---
# yaml-language-server: $schema=https://raw.githubusercontent.com/bjw-s-labs/helm-charts/main/charts/other/app-template/schemas/helmrelease-helm-v2.schema.json
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: coqui-tts
spec:
  chartRef:
    kind: OCIRepository
    name: app-template
  dependsOn: []
  interval: 15m
  values:
    controllers:
      coqui-tts:
        annotations:
          reloader.stakater.com/auto: "true"
        containers:
          app:
            image:
              repository: ghcr.io/idiap/coqui-tts-cpu
              tag: v0.27.3@sha256:ed6ebacb9acb20658223e4a3ab48196d7a3f566749c0d3acb3c66cc0eeb76293
            command:
              - /bin/bash
              - -c
              - >
                tts-server --model_name tts_models/multilingual/multi-dataset/xtts_v2
            env:
              COQUI_TOS_AGREED: 1
            resources:
              requests:
                cpu: 100m
                memory: 512M
              limits:
                memory: 8Gi
    service:
      app:
        ports:
          http:
            port: 5002
    persistence:
      models:
        existingClaim: "{{ .Release.Name }}"
        globalMounts:
          - path: /root/.local/share/tts
