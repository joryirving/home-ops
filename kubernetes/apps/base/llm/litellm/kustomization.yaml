---
# yaml-language-server: $schema=https://json.schemastore.org/kustomization
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ./dashboard
  - ./models
  - ./virtualkeys
  - ./externalsecret.yaml
  - ./grafanadashboard.yaml
  - ./litellmproxy.yaml
  - ./llmkube-skirk-cache.yaml
  - ./muse-glimmer.yaml
  # - ./nemotron-3.5-lightning-30b-a3b.yaml
  - ./nvidia-pool.yaml
  # - ./qwen3.6-35b-a3b.yaml
  - ./qwen3.8-27b.yaml
  # - ./glm-5.3-flash-local.yaml  # commented out for the flash-next swap-back test; skirk holds ONE of these at a time
  # - ./qwen3.8-flash-next.yaml  # ROCm mainline-quant (40a9f4d0); swap back here if gufo regresses
  - ./qwen3.8-flash-next-gufo.yaml
  # - ./gemma-4-e4b.yaml  # vision sidecar; only needed while the Strix lane is text-only (GLM). flash-next has its own mmproj
  # - ./qwen3.8-flash-next-vulkan.yaml  # vulkan fallback (0.5.5); swap here if ROCm regresses
  - ./prometheusrule.yaml
  - ./servicemonitor.yaml
