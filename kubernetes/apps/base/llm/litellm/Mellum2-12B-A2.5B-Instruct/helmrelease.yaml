---
# yaml-language-server: $schema=https://raw.githubusercontent.com/bjw-s-labs/helm-charts/main/charts/other/app-template/schemas/helmrelease-helm-v2.schema.json
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: llama-review
spec:
  chartRef:
    kind: OCIRepository
    name: app-template
  interval: 15m
  dependsOn: []
  values:
    defaultPodOptions:
      hostIPC: true
      nodeSelector:
        topology.kubernetes.io/gpus: amd
      tolerations:
        - key: llm-workload
          operator: Exists
          effect: NoSchedule
    controllers:
      llama-review:
        annotations:
          reloader.stakater.com/auto: "true"
        initContainers:
          model-download:
            image:
              repository: python
              tag: 3.14-slim
            command:
              - /bin/sh
              - -ec
              - |
                set -euo pipefail

                if [ ! -s /models/mellum2-12b-a2.5b/Mellum2-12B-A2.5B-Instruct-Q6_K.gguf ]; then
                  pip install --no-cache-dir "huggingface_hub[hf_transfer]"
                  mkdir -p /models/mellum2-12b-a2.5b
                  rm -rf /models/mellum2-12b-a2.5b/.cache/huggingface/download/Mellum2-12B-A2.5B-Instruct-Q6_K.gguf.lock
                  hf download \
                    JetBrains/Mellum2-12B-A2.5B-Instruct-GGUF-Q6_K \
                    Mellum2-12B-A2.5B-Instruct-Q6_K.gguf \
                    --local-dir /models/mellum2-12b-a2.5b
                  echo "Mellum2-12B-A2.5B-Instruct-Q4_K_M download complete"
                else
                  echo "Mellum2-12B-A2.5B-Instruct-Q4_K_M already downloaded, skipping"
                fi
            env:
              HF_HUB_ENABLE_HF_TRANSFER: "1"
            envFrom:
              - secretRef:
                  name: huggingface
            resources:
              requests:
                cpu: 250m
                memory: 512Mi
              limits:
                memory: 10Gi
        containers:
          app:
            image:
              repository: docker.io/kyuz0/amd-strix-halo-toolboxes
              tag: rocm-7.2.4@sha256:1e8698bd016898f34ab3f8fa45d244ab26da48faca0e56e04f41cdbf3d8a8fc6
              pullPolicy: Always
            securityContext:
              privileged: true
            command:
              - llama-server
            args:
              - --host
              - 0.0.0.0
              - --port
              - "8080"
              - --alias
              - review
              - --model
              - /models/mellum2-12b-a2.5b/Mellum2-12B-A2.5B-Instruct-Q6_K.gguf
              - --ctx-size
              - "131072"
              - --n-gpu-layers
              - all
              - --flash-attn
              - "on"
              - --parallel
              - "1"
              - --cont-batching
              - -sps
              - "0.90"
              - --cache-prompt
              - --kv-unified
              - --metrics
              - --temp
              - "0.6"
              - --top-p
              - "0.95"
              - --top-k
              - "20"
              - --min-p
              - "0.05"
              - --presence-penalty
              - "0"
              - --repeat-penalty
              - "1.0"
              - --batch-size
              - "6144"
              - --ubatch-size
              - "2048"
              - --threads
              - "6"
              - --threads-batch
              - "12"
              - --cache-type-k
              - f16
              - --cache-type-v
              - f16
              - --no-mmap
            resources:
              requests:
                cpu: 1
                memory: 20Gi
              limits:
                memory: 40Gi
            probes:
              liveness:
                enabled: true
                custom: true
                spec:
                  httpGet:
                    path: /health
                    port: 8080
                  initialDelaySeconds: 30
                  periodSeconds: 30
                  failureThreshold: 6
              readiness:
                enabled: true
                custom: true
                spec:
                  httpGet:
                    path: /health
                    port: 8080
                  initialDelaySeconds: 20
                  periodSeconds: 10
                  failureThreshold: 6
              startup:
                enabled: true
                custom: true
                spec:
                  httpGet:
                    path: /health
                    port: 8080
                  failureThreshold: 360
                  periodSeconds: 10
    persistence:
      models:
        existingClaim: llm-models
        globalMounts:
          - path: /models
      dev-dri:
        type: hostPath
        hostPath: /dev/dri
        globalMounts:
          - path: /dev/dri
      dev-kfd:
        type: hostPath
        hostPath: /dev/kfd
        globalMounts:
          - path: /dev/kfd
    service:
      app:
        type: LoadBalancer
        annotations:
          lbipam.cilium.io/ips: 10.69.10.21
        externalTrafficPolicy: Cluster
        ports:
          http:
            port: 8080
