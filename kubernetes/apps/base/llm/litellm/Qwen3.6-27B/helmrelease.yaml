---
# yaml-language-server: $schema=https://raw.githubusercontent.com/bjw-s-labs/helm-charts/main/charts/other/app-template/schemas/helmrelease-helm-v2.schema.json
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: llama-nvidia
spec:
  chartRef:
    kind: OCIRepository
    name: app-template
  interval: 15m
  dependsOn: []
  values:
    defaultPodOptions:
      hostIPC: true
      runtimeClassName: nvidia
      nodeSelector:
        topology.kubernetes.io: egpu
    controllers:
      llama-nvidia:
        annotations:
          reloader.stakater.com/auto: "true"
        initContainers:
          model-download:
            image:
              repository: python
              tag: 3.14-slim
            command:
              - /bin/sh
              - -ec
              - |
                set -euo pipefail

                if [ ! -s /models/qwen3.6-27b/Qwen3.6-27B-UD-Q3_K_XL.gguf ] || [ ! -s /models/qwen3.6-27b/mmproj-F16.gguf ]; then
                  pip install --no-cache-dir "huggingface_hub[hf_transfer]"
                  mkdir -p /models/qwen3.6-27b
                  rm -rf /models/qwen3.6-27b/.cache/huggingface/download/Qwen3.6-27B-UD-Q3_K_XL.gguf.lock
                  hf download \
                    unsloth/Qwen3.6-27B-MTP-GGUF \
                    Qwen3.6-27B-UD-Q3_K_XL.gguf \
                    mmproj-F16.gguf \
                    --local-dir /models/qwen3.6-27b

                  echo "Qwen3.6-27B download complete"
                else
                  echo "Qwen3.6-27B already downloaded, skipping"
                fi
            env:
              HF_HUB_ENABLE_HF_TRANSFER: "1"
            envFrom:
              - secretRef:
                  name: huggingface
            resources:
              requests:
                cpu: 250m
                memory: 512Mi
              limits:
                memory: 4Gi
        containers:
          app:
            image:
              repository: ghcr.io/ggml-org/llama.cpp
              tag: server-cuda-b9246
            securityContext:
              privileged: true
            args:
              - --host
              - 0.0.0.0
              - --port
              - "8080"
              - --alias
              - nvidia
              - --model
              - /models/qwen3.6-27b/Qwen3.6-27B-UD-Q3_K_XL.gguf
              - --mmproj
              - /models/qwen3.6-27b/mmproj-F16.gguf
              - --ctx-size
              - "131072"
              - --n-gpu-layers
              - "99"
              - --flash-attn
              - "on"
              - --parallel
              - "1"
              - --cont-batching
              - --spec-type
              - draft-mtp
              - --spec-draft-p-min
              - "0.75"
              - --spec-draft-n-max
              - "3"
              - --spec-draft-type-k
              - "q8_0"
              - --spec-draft-type-v
              - "q8_0"
              - -sps
              - "0.90"
              - --cache-prompt
              - --cache-ram
              - "16384"
              - --kv-unified
              - --image-min-tokens
              - "1024"
              - --metrics
              - --temp
              - "0.6"
              - --top-p
              - "0.95"
              - --top-k
              - "20"
              - --min-p
              - "0"
              - --presence-penalty
              - "0.5"
              - --repeat-penalty
              - "1.03"
              - --reasoning-budget
              - "16384"
              - --reasoning-budget-message
              - "Stop reasoning and produce the implementation, tool call, or final answer."
              - --batch-size
              - "4096"
              - --ubatch-size
              - "1024"
              - --threads
              - "6"
              - --threads-batch
              - "12"
              - --cache-type-k
              - q8_0
              - --cache-type-v
              - q8_0
              - --mmap
              - --jinja
              - --chat-template-kwargs
              - '{"preserve_thinking":true}'
            env:
              NVIDIA_DRIVER_CAPABILITIES: compute,utility
            resources:
              requests:
                cpu: 500m
                memory: 16Gi
              limits:
                memory: 32Gi
                nvidia.com/gpu: 1
            probes:
              liveness:
                enabled: true
                custom: true
                spec:
                  httpGet:
                    path: /health
                    port: 8080
                  initialDelaySeconds: 30
                  periodSeconds: 30
                  failureThreshold: 6
              readiness:
                enabled: true
                custom: true
                spec:
                  httpGet:
                    path: /health
                    port: 8080
                  initialDelaySeconds: 20
                  periodSeconds: 10
                  failureThreshold: 6
              startup:
                enabled: true
                custom: true
                spec:
                  httpGet:
                    path: /health
                    port: 8080
                  failureThreshold: 360
                  periodSeconds: 10
    persistence:
      models:
        existingClaim: llm-models-rook
        globalMounts:
          - path: /models
    service:
      app:
        ports:
          http:
            port: 8080
