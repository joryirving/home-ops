---
apiVersion: v1
kind: ConfigMap
metadata:
  name: llama-swap-config-single
  namespace: llm
data:
  config.yaml: |
    windowSize: 5
    logLevel: "DEBUG"
    models:
      qwen27b:
        cmd: >-
          /usr/local/bin/llama-server
          --host 127.0.0.1
          --port 9001
          --model /models/BF16/Qwen3.5-27B-BF16-00001-of-00002.gguf
          --mmproj /models/qwen27b-mmproj-F32.gguf
          --ctx-size 131072
          --n-gpu-layers 99
          --flash-attn on
          --parallel 4
          --cont-batching
          --cache-prompt
          --temp 0.5
          --top-p 0.95
          --top-k 20
          --min-p 0
          --batch-size 2048
          --mmap
          --numa distribute
          --chat-template chatml
          --jinja
          --swa-full
        proxy: http://127.0.0.1:9001
        ttl: 0
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: llama-swap-config-dual
  namespace: llm
data:
  config.yaml: |
    windowSize: 5
    logLevel: "DEBUG"
    groups:
      always-on:
        swap: false
        exclusive: false
        members:
          - qwen35b
          - qwen3-coder
    models:
      qwen35b:
        cmd: >-
          /usr/local/bin/llama-server
          --host 127.0.0.1
          --port 9001
          --model /models/Qwen3.5-35B-A3B-heretic.Q4_K_M.gguf
          --mmproj /models/mmproj-F32.gguf
          --ctx-size 131072
          --n-gpu-layers 99
          --flash-attn on
          --parallel 2
          --cont-batching
          --cache-prompt
          --temp 0.2
          --top-p 0.95
          --top-k 20
          --min-p 0
          --cache-type-k q8_0
          --cache-type-v q8_0
          --batch-size 2048
          --mmap
          --numa distribute
          --chat-template-file /config/qwen-tool-use.jinja
          --reasoning-budget 512
          --jinja
          --chat-template-kwargs {"parse_tool_calls":true}
          --swa-full
        proxy: http://127.0.0.1:9001
        ttl: 0
        group: qwen

      qwen3-coder:
        cmd: >-
          /usr/local/bin/llama-server
          --host 127.0.0.1
          --port 9002
          --model /models/Qwen3-Coder-Next-UD-Q4_K_XL.gguf
          --ctx-size 131072
          --n-gpu-layers 99
          --flash-attn on
          --parallel 2
          --cont-batching
          --cache-prompt
          --cache-type-k q8_0
          --cache-type-v q8_0
          --batch-size 2048
          --mmap
          --numa distribute
          --temp 0.2
          --top-p 0.95
          --top-k 20
          --jinja
          --chat-template-file /config/qwen-tool-use.jinja
          --chat-template-kwargs {"parse_tool_calls":true}
          --reasoning-budget 0
        proxy: http://127.0.0.1:9002
        ttl: 0
        group: coder
  qwen-tool-use.jinja: |
    {%- if tools %}
        {{- '<|im_start|>system\n' }}
        {%- if messages[0]['role'] == 'system' %}
            {{- messages[0]['content'] }}
        {%- else %}
            {{- 'You are Qwen, created by Alibaba Cloud. You are a helpful assistant.' }}
        {%- endif %}
        {{- "\n\n# Tools\n\nYou may call one or more functions to assist with the user query.\n\nYou are provided with function signatures within <tools></tools> XML tags:\n<tools>" }}
        {%- for tool in tools %}
            {{- "\n" }}
            {{- tool | tojson }}
        {%- endfor %}
        {{- "\n</tools>\n\nFor each function call, return a json object with function name and arguments within <tool_call></tool_call> XML tags:\n<tool_call>\n{\"name\": <function-name>, \"arguments\": <args-json-object>}\n</tool_call><|im_end|>\n" }}
    {%- else %}
        {%- if messages[0]['role'] == 'system' %}
            {{- '<|im_start|>system\n' + messages[0]['content'] + '<|im_end|>\n' }}
        {%- else %}
            {{- '<|im_start|>system\nYou are Qwen, created by Alibaba Cloud. You are a helpful assistant.<|im_end|>\n' }}
        {%- endif %}
    {%- endif %}
    {%- for message in messages %}
        {%- if (message.role == "user") or (message.role == "system" and not loop.first) or (message.role == "assistant" and not message.tool_calls) %}
            {{- '<|im_start|>' + message.role + '\n' + message.content + '<|im_end|>' + '\n' }}
        {%- elif message.role == "assistant" %}
            {{- '<|im_start|>' + message.role }}
            {%- if message.content %}
                {{- '\n' + message.content }}
            {%- endif %}
            {%- for tool_call in message.tool_calls %}
                {%- if tool_call.function is defined %}
                    {%- set tool_call = tool_call.function %}
                {%- endif %}
                {{- '\n<tool_call>\n{"name": "' }}
                {{- tool_call.name }}
                {{- '", "arguments": ' }}
                {{- tool_call.arguments | tojson }}
                {{- '}\n</tool_call>' }}
            {%- endfor %}
            {{- '<|im_end|>\n' }}
        {%- elif message.role == "tool" %}
            {%- if (loop.index0 == 0) or (messages[loop.index0 - 1].role != "tool") %}
                {{- '<|im_start|>user' }}
            {%- endif %}
            {{- '\n<tool_response>\n' }}
            {{- message.content }}
            {{- '\n</tool_response>' }}
            {%- if loop.last or (messages[loop.index0 + 1].role != "tool") %}
                {{- '<|im_end|>\n' }}
            {%- endif %}
        {%- endif %}
    {%- endfor %}
    {%- if add_generation_prompt %}
        {{- '<|im_start|>assistant\n' }}
    {%- endif %}
