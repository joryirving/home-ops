---
apiVersion: v1
kind: ConfigMap
metadata:
  name: sabnzbd-gatus-ep
  namespace: downloads
  labels:
    gatus.io/enabled: "true"
data:
  config.yaml: |
    endpoints:
      - name: sabnzbd
        group: downloads
        url: 1.1.1.1
        interval: 1m
        dns:
          query-name: sabnzbd.${SECRET_DOMAIN}
          query-type: A
        conditions:
          - "len([BODY]) == 0"
        ui:
          hide-hostname: true
