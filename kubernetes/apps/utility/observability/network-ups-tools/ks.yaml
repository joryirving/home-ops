---
# yaml-language-server: $schema=https://kube-schemas.pages.dev/kustomize.toolkit.fluxcd.io/kustomization_v1.json
apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: &app network-ups-tools
spec:
  commonMetadata:
    labels:
      app.kubernetes.io/name: *app
  interval: 30m
  path: ./kubernetes/apps/base/observability/network-ups-tools/app
  postBuild:
    substitute:
      APP: *app
      HOSTNAME: network-nut
  prune: true
  sourceRef:
    kind: GitRepository
    name: flux-system
    namespace: flux-system
  targetNamespace: observability
  timeout: 5m
  wait: false
  patches:
  - target:
      kind: HelmRelease
      name: network-ups-tools
    patch: |-
      apiVersion: helm.toolkit.fluxcd.io/v2
      kind: HelmRelease
      metadata:
        name: network-ups-tools
      spec:
        values:
          controllers:
            network-ups-tools:
              pod:
                nodeSelector:
                  ups.feature.node.kubernetes.io/apc: "true"
