# yaml-language-server: $schema=https://k8s-schemas.home-operations.com/kustomize.toolkit.fluxcd.io/kustomization_v1.json
apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: &app agregarr
spec:
  components:
    - ../../../../../components/kopiur/backup
    - ../../../../../components/zeroscaler
  interval: 1h
  path: ./kubernetes/apps/base/media/plex/agregarr
  postBuild:
    substitute:
      APP: *app
  wait: false
  prune: true
  sourceRef:
    kind: GitRepository
    name: flux-system
    namespace: flux-system
---
# yaml-language-server: $schema=https://k8s-schemas.home-operations.com/kustomize.toolkit.fluxcd.io/kustomization_v1.json
apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: &app plex
spec:
  components:
    - ../../../../../components/gpu
    - ../../../../../components/igpu-spread
    - ../../../../../components/kopiur/backup
    - ../../../../../components/zeroscaler
  interval: 1h
  path: ./kubernetes/apps/base/media/plex/app
  postBuild:
    substitute:
      APP: *app
      KOPIUR_CAPACITY: 50Gi
  wait: false
  prune: true
  sourceRef:
    kind: GitRepository
    name: flux-system
    namespace: flux-system
# ---
# # yaml-language-server: $schema=https://k8s-schemas.home-operations.com/kustomize.toolkit.fluxcd.io/kustomization_v1.json
# apiVersion: kustomize.toolkit.fluxcd.io/v1
# kind: Kustomization
# metadata:
#   name: &app kometa
# spec:
#   components:
#     - ../../../../../components/volsync
#   dependsOn:
#     - name: radarr
#       namespace: downloads
#     - name: sonarr
#       namespace: downloads
#     - name: plex
#     - name: tautulli
#   interval: 1h
#   path: ./kubernetes/apps/base/media/plex/kometa
#   postBuild:
#     substitute:
#       APP: *app
#       VOLSYNC_CAPACITY: 10Gi
#   prune: true
#   sourceRef:
#     kind: GitRepository
#     name: flux-system
#     namespace: flux-system
#   wait: false
---
# yaml-language-server: $schema=https://k8s-schemas.home-operations.com/kustomize.toolkit.fluxcd.io/kustomization_v1.json
apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: plex-auto-languages
spec:
  dependsOn:
    - name: plex
  interval: 1h
  path: ./kubernetes/apps/base/media/plex/plex-auto-languages
  wait: false
  prune: true
  sourceRef:
    kind: GitRepository
    name: flux-system
    namespace: flux-system
---
# yaml-language-server: $schema=https://k8s-schemas.home-operations.com/kustomize.toolkit.fluxcd.io/kustomization_v1.json
apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: &app imagemaid
spec:
  dependsOn:
    - name: plex
  interval: 1h
  path: ./kubernetes/apps/base/media/plex/imagemaid
  postBuild:
    substitute:
      APP: *app
  wait: false
  prune: true
  sourceRef:
    kind: GitRepository
    name: flux-system
    namespace: flux-system
---
# yaml-language-server: $schema=https://k8s-schemas.home-operations.com/kustomize.toolkit.fluxcd.io/kustomization_v1.json
apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: &app scrob
spec:
  components:
    - ../../../../../components/postgres
    - ../../../../../components/kopiur/backup
  dependsOn:
    - name: plex
  healthCheckExprs:
    - apiVersion: postgresql.cnpg.io/v1
      kind: Cluster
      failed: status.conditions.filter(e, e.type == 'Ready').all(e, e.status == 'False')
      current: status.conditions.filter(e, e.type == 'Ready').all(e, e.status == 'True')
  interval: 1h
  path: ./kubernetes/apps/base/media/plex/scrob
  patches:
    - # PostgreSQL 18 canary; keep PostgreSQL 17 recovery data in its old archive.
      patch: |-
        - op: replace
          path: /spec/imageName
          value: ghcr.io/cloudnative-pg/postgresql:18.6-202608240816-system-bullseye@sha256:a66f9e4c34950b78668fa4741bf55e6a58227cc31a6e60c128f2ce1d4c072928
        - op: replace
          path: /spec/backup/barmanObjectStore/serverName
          value: scrob-pg18
        - op: replace
          path: /spec/externalClusters/0/barmanObjectStore/serverName
          value: scrob-pg18
      target:
        group: postgresql.cnpg.io
        kind: Cluster
        version: v1
  postBuild:
    substitute:
      APP: *app
      KOPIUR_CAPACITY: 1Gi
  wait: false
  prune: true
  sourceRef:
    kind: GitRepository
    name: flux-system
    namespace: flux-system
