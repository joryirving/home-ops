---
apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: tf-controller
  namespace: kube-tools
spec:
  interval: 30m
  chart:
    spec:
      chart: tf-controller
      version: 0.12.0
      sourceRef:
        kind: HelmRepository
        name: tf-controller
        namespace: flux-system
      interval: 30m
  values:
    metrics:
      enabled: true
      serviceMonitor:
        enabled: true
        interval: 1m
