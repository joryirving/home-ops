---
apiVersion: externaldns.k8s.io/v1alpha1
kind: DNSEndpoint
metadata:
  name: omni
spec:
  endpoints:
    - dnsName: omni.${SECRET_DOMAIN}
      recordType: CNAME
      targets: ["${AUTH0_OMNI}"] # Auth0
