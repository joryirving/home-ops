---
apiVersion: externaldns.k8s.io/v1alpha1
kind: DNSEndpoint
metadata:
  name: vibecraft
spec:
  endpoints:
    - dnsName: "vibecraft.${SECRET_DOMAIN}"
      recordType: CNAME
      targets: ["mc.${SECRET_DOMAIN}"]
