# DISABLED PROVIDERS

- **anthropic**: intentionally omitted (no subscription, no API key)  
- All model fallbacks restricted to `qwen-*` providers only  
- Errors referencing "No API key for provider 'anthropic'" are now suppressed by config enforcement